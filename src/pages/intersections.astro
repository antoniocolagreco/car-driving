---
import DefaultLayout from '../layouts/DefaultLayout.astro'
---

<script>
    import { init } from '../libs/trigonometry'
    document.addEventListener('astro:page-load', () => {
        const container = document.querySelector('#intersections-container') as HTMLDivElement | undefined
        if (container) init(container)
    })
</script>

<DefaultLayout title='Intersections'>
    <div class='h-full overflow-hidden relative' id='intersections-container'></div>
</DefaultLayout>
