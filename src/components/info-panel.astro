---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'div'>
const { class: className, ...rest } = Astro.props
---

<div class:list={['', className]} class="flex flex-col gap-1 p-1 w-30" {...rest}>
    <slot />
</div>
