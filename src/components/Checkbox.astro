---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'input'>

const { class: className, ...otherProps } = Astro.props
---

<input type='checkbox' class:list={['', className]} class='text-stone-800' {...otherProps} />
