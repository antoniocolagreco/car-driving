---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'a'>

const { class: className, ...otherProps } = Astro.props
---

<a
    class:list={['text-white hover:text-white/70 font-semibold capitalize', className]}
    {...otherProps}
>
    <slot />
</a>
