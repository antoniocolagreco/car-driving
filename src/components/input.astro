---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'input'>

const { class: className, ...otherProps } = Astro.props
---

<input
    class:list={['', className]}
    class="text-stone-800 px-2 py-1 rounded-md font-bold"
    {...otherProps}
/>
